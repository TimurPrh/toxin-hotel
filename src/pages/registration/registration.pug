extends ../template/template.pug

block content
  include ../../blocks/header/header.pug
  include ../../blocks/registration-card/registration-card.pug
  include ../../blocks/footer/footer.pug
  section.landing-main
    .landing-header 
      +header({login: false})
    .container
      .landing-main__register
        +registration-card({header: "Регистрация аккаунта"})

  section.landing-footer
    +footer({mobile: false})